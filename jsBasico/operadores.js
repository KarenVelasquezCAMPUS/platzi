// Operadores de Operación 
    // Operadores Binarios, un operador generando algo entre dos variables 

        3 + 2
        50 - 10
        10 * 20
        10 / 2
        
// Operadores Unitarios 
    // Un solo operador trabajando con un solo valor 
        ! /* = false */
        !false
        /* true, la respuesta es true porque estamos confirmando que ! es false */

// Operadores de Asignación 
    // se inicializa asignandole el valor = 
        var a = 1;

        //comparar

        5 < 3  
        /* false */
        5 > 3
        /* true */
        5 <= 6
        /* true */
        5 >= 6 
        /* false */


        a && b /* las dos deben de ser verdad para que se cumpla una condición*/
        a || b /* alguna de las dos debe ser verdad para que se cumpla la condición, más flexible */


        var edad = 40 
        edad++
        /* incrementa por 1 */

        edad += 2
        /* incrementa por 2 o por lo que se le asigne incrementar*/

/* 
    = asigna 
    == compara 
    === validar igualdad
*/